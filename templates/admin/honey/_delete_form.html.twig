<form method="post" action="{{ path('app_admin_honey_delete', {'id': honey.id}) }}" onsubmit="return confirm('Voulez vous supprimer ce type ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ honey.id) }}">
    <button class="form-button">Supprimer</button>
</form>