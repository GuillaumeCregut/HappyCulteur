<form method="post" action="{{ path('app_admin_disease_delete', {'id': disease.id}) }}" onsubmit="return confirm('Voulez vous supprimer cette maladie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disease.id) }}">
    <button class="form-button">Supprimer</button>
</form>