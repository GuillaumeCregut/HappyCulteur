{{ form_start(form) }}
    {{ form_row(form.name,  {
            row_attr: {
                class: 'admin-desease-form-row'
            },
            label: 'Nom : ',
            attr : {
                class: 'admin-desease-form-input'
            }
    }) }}
<button class="form-button">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}