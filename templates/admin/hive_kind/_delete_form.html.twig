<form method="post" action="{{ path('app_admin_hive_kind_delete', {'id': hive_kind.id}) }}" onsubmit="return confirm('Voulez vous supprimer ce type ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hive_kind.id) }}">
    <button class="form-button">Supprimer</button>
</form>
