<form method="post" action="{{ path('app_admin_hive_rise_delete', {'id': hive_rise.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hive_rise.id) }}">
    <button class="form-button">Supprimer</button>
</form>
