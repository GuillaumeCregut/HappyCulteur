{{ form_start(form, {
    action: path('app_param_disease_add'),
    attr: {'data-turbo': 'false'}
}) }}
    {{ form_row(form.name,  {
            row_attr: {
                class: 'visit-form-row'
            },
            label: 'Nom : ',
            attr : {
                class: 'visit-form-input'
            }
    }) }}
<button class="form-button">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}