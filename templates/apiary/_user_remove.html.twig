<form method="post" action="{{ path('app_apiary_user_delete', {'id': beekeeper.id}) }}" onsubmit="return confirm('Voulez vous supprimer cet utilisateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beekeeper.id) }}">
    <button class="form-button">Supprimer</button>
</form>