{% extends 'base.html.twig' %}
{% block stylesheets %}
	<link rel="stylesheet" href="{{asset('styles/default/stats/global.css')}}">
{% endblock %}
{% block title %}{% endblock %}

{% block body %}
<h1 class="title">Relevé des récoltes</h1>
<p class="stats-line">Ruche : {{ hive.name }}</p>
{{ form_start(form) }}
{{ form_errors(form) }}
     <twig:StatsDates 
     startDate="{{ form.startDate}}"
     endDate="{{ form.endDate}}"
     typePage="récoltes"
     />
{{ form_end(form) }}

<p class="stats-btn-container"><a href="{{ path('app_stats_index', {id: hive.id}) }}" class= "form-button">Retour aux statistiques</a></p>
{% endblock %}