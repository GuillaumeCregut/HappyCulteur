<form method="post" action="{{ path('app_purchase_delete', {'id': purchase.id}) }}" onsubmit="return confirm('Voulez vous supprimer cet achat ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ purchase.id) }}">
    <button class="form-button">Supprimer</button>
</form>