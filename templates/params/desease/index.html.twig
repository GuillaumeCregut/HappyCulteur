{% extends 'base.html.twig' %}

{% block stylesheets %}
	<link rel="stylesheet" href="{{asset('styles/default/params/desease.css')}}">
{% endblock %}
{% block title %}Maladies{% endblock %}

{% block body %}
<h1 class="title">Gestion des maladies</h1>
<p>Liste des maladies existantes</p>
<ul class="param-desease-container">
{% for desease in deseases %}
<li class = "param-desease-line">{{ desease.name}}</li>
{% else %}
<li class = "param-desease-line">Aucune maladie trouvée</li>
{% endfor %}
</ul>
{{ form_start(form) }}
<fieldset class="param-desease-fieldset">
    <legend>Ajouter une maladie</legend>
    {{ form_row(form.name, {
        label: 'Nom : ',
        attr: {
            class: 'param-desease-field'
        }
    }) }}
    <button class="form-button">Valider</button>
</fieldset>
{{ form_end(form) }}
<p class="param-desease-btn-container"><a href="{{ path('app_params') }}" class="form-button">Retour aux paramètres</a></p>
{% endblock %}
